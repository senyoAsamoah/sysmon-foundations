# Project 1 — Linux Basics & System Setup

## 🎯 Objectives
- Navigate the Linux filesystem
- Create users, directories, and manage permissions
- Install system packages
- Understand basic system logs

## 🧪 Tasks
1. Create directories:
   - `/opt/sysmon`
   - `/opt/sysmon/logs`
2. Create user `sysmon_user`
3. Install essential tools: `sudo apt install htop curl net-tools tree git -y`
4. Explore:
- `ls`, `cd`, `mv`, `chmod`, `chown`
- `ps`, `top`, `systemctl`
- `journalctl`, `dmesg`

## 📦 Deliverables
- Notes in this file
- Commands used
